<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.control.TableColumn?>
<?import javafx.scene.control.TableView?>
<?import javafx.scene.control.TextField?>
<?import javafx.scene.control.Tooltip?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.VBox?>

<VBox prefHeight="700.0" prefWidth="1200.0" spacing="0" style="-fx-background-color: linear-gradient(to bottom, #f8f9fa, #e9ecef);" xmlns="http://javafx.com/javafx/25" xmlns:fx="http://javafx.com/fxml/1" fx:controller="businessmanager.surface.CheckCSPController">

    <!-- Barra superior -->
    <VBox style="-fx-background-color: white; -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.1), 5, 0, 0, 2);">
        <padding>
            <Insets bottom="15" left="25" right="25" top="15" />
        </padding>

        <HBox alignment="CENTER_LEFT" spacing="20">
            <Button onAction="#goBack" style="-fx-background-color: transparent; -fx-text-fill: #6c757d;                           -fx-font-size: 20; -fx-cursor: hand; -fx-padding: 5 10;" text="⬅">
                <tooltip>
                    <Tooltip text="Volver al panel principal" />
                </tooltip>
            </Button>

            <VBox spacing="5">
                <Label fx:id="labelCSP" style="-fx-font-size: 24px; -fx-font-weight: bold;                        -fx-text-fill: #2c3e50;" text="Clientes" />
                <Label fx:id="lblSubtitle" style="-fx-font-size: 14px; -fx-text-fill: #6c757d;" text="Gestión completa" />
            </VBox>

            <Region HBox.hgrow="ALWAYS" />

            <HBox alignment="CENTER_RIGHT" spacing="10">
                <TextField fx:id="searchField" promptText="Buscar..." style="-fx-background-radius: 20; -fx-border-radius: 20;                                  -fx-border-color: #ced4da; -fx-padding: 8 15;                                  -fx-pref-width: 250;" />
                <Button style="-fx-background-color: transparent;                          -fx-text-fill: #6c757d; -fx-cursor: hand;                          -fx-padding: 5;" text="🔍" />
                <Label fx:id="lblTotal" style="-fx-font-size: 14px; -fx-text-fill: #6c757d;" text="Total: 0" />
            </HBox>
        </HBox>
    </VBox>

    <!-- Panel de botones de acción -->
    <HBox spacing="15" style="-fx-padding: 20 25;">
        <Button fx:id="buttonAdd" onAction="#addCSP" style="-fx-background-color: #28a745; -fx-text-fill: white;                       -fx-font-size: 14px; -fx-font-weight: bold;                       -fx-background-radius: 8; -fx-cursor: hand;                       -fx-padding: 10 20;" text="➕ Añadir">
            <tooltip>
                <Tooltip text="Añadir nuevo elemento" />
            </tooltip>
        </Button>

        <Button fx:id="buttonEdit" onAction="#editCSP" style="-fx-background-color: #17a2b8; -fx-text-fill: white;                       -fx-font-size: 14px; -fx-font-weight: bold;                       -fx-background-radius: 8; -fx-cursor: hand;                       -fx-padding: 10 20;" text="Editar">
            <tooltip>
                <Tooltip text="Editar elemento seleccionado" />
            </tooltip>
        </Button>

        <Button fx:id="buttonDelete" onAction="#deleteCSP" style="-fx-background-color: #dc3545; -fx-text-fill: white;                       -fx-font-size: 14px; -fx-font-weight: bold;                       -fx-background-radius: 8; -fx-cursor: hand;                       -fx-padding: 10 20;" text="Eliminar">
            <tooltip>
                <Tooltip text="Eliminar elemento seleccionado" />
            </tooltip>
        </Button>

        <Button fx:id="buttonExport" style="-fx-background-color: #6c757d; -fx-text-fill: white;                       -fx-font-size: 14px; -fx-font-weight: bold;                       -fx-background-radius: 8; -fx-cursor: hand;                       -fx-padding: 10 20;" text="📤 Exportar">
            <tooltip>
                <Tooltip text="Exportar datos a CSV/Excel" />
            </tooltip>
        </Button>

        <Button fx:id="buttonRefresh" style="-fx-background-color: #6f42c1; -fx-text-fill: white;                       -fx-font-size: 14px; -fx-font-weight: bold;                       -fx-background-radius: 8; -fx-cursor: hand;                       -fx-padding: 10 20;" text="🔄 Actualizar">
            <tooltip>
                <Tooltip text="Actualizar lista" />
            </tooltip>
        </Button>

        <Region HBox.hgrow="ALWAYS" />

        <Button fx:id="buttonInspect" onAction="#inspectCSP" style="-fx-background-color: #007bff; -fx-text-fill: white;                       -fx-font-size: 14px; -fx-font-weight: bold;                       -fx-background-radius: 8; -fx-cursor: hand;                       -fx-padding: 10 25;" text="👁 Ver Detalles">
            <tooltip>
                <Tooltip text="Ver detalles del elemento seleccionado" />
            </tooltip>
        </Button>
    </HBox>

    <!-- Tabla principal -->
    <TableView fx:id="tableView" style="-fx-background-color: white; -fx-background-radius: 10;                      -fx-border-radius: 10; -fx-border-color: #dee2e6;                      -fx-effect: dropshadow(three-pass-box, rgba(0,0,0,0.08), 8, 0, 0, 2);                      -fx-margin: 0 25;" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="colCodigo" prefWidth="80" text="Código" />
            <TableColumn fx:id="colNombre" prefWidth="200" text="Nombre" />
            <TableColumn fx:id="colNif" prefWidth="120" text="NIF/CIF" />
            <TableColumn fx:id="colEmail" prefWidth="180" text="Email" />
            <TableColumn fx:id="colTelefono" prefWidth="120" text="Teléfono" />
            <TableColumn fx:id="colDireccion" prefWidth="200" text="Dirección" />
            <TableColumn fx:id="colCiudad" prefWidth="120" text="Ciudad" />
            <TableColumn fx:id="colEstado" prefWidth="100" text="Estado" />
        </columns>
        <columnResizePolicy>
            <TableView fx:constant="CONSTRAINED_RESIZE_POLICY" />
        </columnResizePolicy>
        <padding>
            <Insets left="25" right="25" />
        </padding>
    </TableView>

    <!-- Barra de estado inferior -->
    <HBox alignment="CENTER_LEFT" spacing="20" style="-fx-padding: 15 25;">
        <Label fx:id="lblSelected" style="-fx-font-size: 13px; -fx-text-fill: #6c757d;" text="Seleccionados: 0" />
        <Region HBox.hgrow="ALWAYS" />
    </HBox>

</VBox>
